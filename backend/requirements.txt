# PHI Shield Backend (FastAPI + Ollama + redaction + Pathway)
# Requires Python 3.11 or 3.12 (Pathway/pyarrow have no wheels for 3.14).
# LLM: Ollama (local); install from https://ollama.com and run `ollama pull llama3.2`.
# Redaction: Microsoft Presidio (presidio-analyzer + presidio-anonymizer); optional: spacy + en_core_web_lg for better NER.
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0
python-dotenv>=1.0.0
pathway>=0.10.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.5.0
